def line_generator(screen, pos1, pos2, color, board_rect, item_rect, ratio)
start_finish = ()
    for index, start in enumerate(pos1):
        end = pos2[index]

            

            
            
            
        pg.draw.lines(screen, color, False, start_finish, 5)